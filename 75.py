class Solution:
    def sortColors(self, nums: List[int]) -> None:
        """
        75. Sort Colors (Dutch National Flag)
        Medium

        三向切分：0=红色, 1=白色, 2=蓝色（原地修改）
        """
        pass
