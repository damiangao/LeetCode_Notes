class Solution:
    def mySqrt(self, x):
        l = 0
        r = x
        ans = -1
        while l <= r:
            mid = (l + r) >> 1
            if mid**2 <= x:
                ans = mid
                l = mid + 1
            else:
                r = mid - 1
        return ans
