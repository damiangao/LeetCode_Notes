class Solution:
    def reconstructQueue(self, people: List[List[int]]) -> List[List[int]]:
        """
        406. Queue Reconstruction by Height
        Medium

        根据身高重建队列（[h, k] h是身高，k是前面身高>=h的人数）
        """
        pass
