# timeover
class Solution:
    def rob(self, nums: List[int]) -> int:
        n = len(nums)
        def dp(i):
            if i < n:
                return max(dp(i+2) + nums[i], dp(i+1))
            else:
                return 0
        return dp(0)
